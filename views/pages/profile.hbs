{{#ifEquals user.type "employer"}}
<div class="container mt-5">
    <h1 class="text-center mb-4">Your Pets</h1>

    {{#each pets}}
    <div class="row justify-content-center mb-3">
        {{> pet_card}}
    </div>
    {{/each}}
</div>
{{else}}
<h1>
<div class="container mt-5">
    <h1 class="text-center mb-4">Your Accepted Jobs</h1>

    {{#each jobs}}
    <div class="row justify-content-center mb-3">
        {{> job_card1}}
    </div>
    {{/each}}
</div>
</h1>

{{/ifEquals}}